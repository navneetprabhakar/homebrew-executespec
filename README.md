# ExecuteSpec Homebrew Tap

Install the ExecuteSpec CLI:

```sh
brew tap navneetprabhakar/executespec
brew install executespec-cli
```

Upgrade:

```sh
brew update
brew upgrade executespec-cli
```

Verify:

```sh
executespec --version
es --version
eslocal --version
```
